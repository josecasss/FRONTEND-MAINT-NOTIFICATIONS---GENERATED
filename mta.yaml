_schema-version: "3.2"
ID: maintnotification
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: maintnotification-destination-content
  type: com.sap.application.content
  requires:
  - name: maintnotification-destination-service
    parameters:
      content-target: true
  - name: maintnotification-repo-host
    parameters:
      service-key:
        name: maintnotification-repo-host-key
  - name: maintnotification-uaa
    parameters:
      service-key:
        name: maintnotification-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: maintnotification_html_repo_host
          ServiceInstanceName: maintnotification-html5-service
          ServiceKeyName: maintnotification-repo-host-key
          sap.cloud.service: maintnotification
        - Authentication: OAuth2UserTokenExchange
          Name: maintnotification_uaa
          ServiceInstanceName: maintnotification-xsuaa-service
          ServiceKeyName: maintnotification-uaa-key
          sap.cloud.service: maintnotification
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: maintnotification-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: maintnotification-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - maintnotification.zip
      name: maintnotification
      target-path: resources/
- name: maintnotification
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: maintnotification-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: maintnotification-destination-service
    service-plan: lite
- name: maintnotification-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: maintnotification-xsuaa-service
    service-plan: application
- name: maintnotification-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: maintnotification-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
